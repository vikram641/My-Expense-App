<?xml version="1.0" encoding="utf-8"?>
<!-- item_legend_row.xml — reusable legend row for the donut chart -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:layout_marginBottom="6dp">

    <!-- Colored dot -->
    <View
        android:id="@+id/viewColor"
        android:layout_width="@dimen/legend_dot_size"
        android:layout_height="@dimen/legend_dot_size"
        android:background="@drawable/bg_legend_dot" />

    <!-- Label + percentage -->
    <TextView
        android:id="@+id/tvLegend"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:layout_marginStart="8dp"
        android:text="Food 39%"
        style="@style/TextAppearance.BudgetTracker.ChartLegend" />
</LinearLayout>
